filters:
  and:
    - type == "task"
    - status != "done"
    - status != "cancelled"
views:
  - type: obtask-feed
    name: Feed
    newItemFolder: Tasks
    order:
      - file.name
      - status
      - due
      - scheduled
      - priority
      - project
      - tags
  - type: obtask-calendar
    name: Calendar
    newItemFolder: Tasks
  - type: table
    name: All tasks
    order:
      - file.name
      - status
      - priority
      - due
      - scheduled
